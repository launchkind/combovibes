// Legacy font stubs — original local font files removed; replaced by Playfair Display + Inter
// These stubs prevent import errors in old template components pending full rebuild
export { inter as integralCF, inter as satoshi } from "@/styles/fonts";
